services:
  marketplace:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - '4201:8080'
    environment:
      PORT: 8080
      MARKETPLACE_GAME_API_BASE_URL: ${MARKETPLACE_GAME_API_BASE_URL:-http://host.docker.internal:9001}
